{
  "name": "creatio-mcp-readonly",
  "version": "0.1.0",
  "private": true,
  "description": "Locked-down, read-only MCP server for a Creatio instance (OData 4, forms auth).",
  "type": "module",
  "bin": {
    "creatio-mcp-readonly": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node --use-system-ca dist/index.js",
    "dev": "tsc && node --use-system-ca dist/index.js",
    "app": "tsc && node --use-system-ca dist/server.js",
    "watch": "tsc --watch --preserveWatchOutput",
    "app:watch": "node --watch --use-system-ca dist/server.js",
    "test-auth": "tsc && node --use-system-ca --env-file=.env dist/test-auth.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "playwright-core": "^1.62.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
